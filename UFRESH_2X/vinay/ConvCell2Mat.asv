for i = 40[256 1024 2048 4096]
    load(sprintf('pyMap%i',i));
    if iscell(Map)
        Map = cellfun(@(x)reshape(x,[],1),Map,'un',0);
        Map = cell2mat(Map);
        save(sprintf('pyMap%icell',i),'Map');
    end
end